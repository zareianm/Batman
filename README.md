# بتمن در گاتهام 🦇

## معرفی پروژه

این پروژه یک شبیه‌ساز گشت شبانه بتمن در شهر گاتهام است که با موتور بازی‌سازی Unity ساخته شده است. در این بازی، شما کنترل بتموبیل را در دست دارید و می‌توانید در سه حالت مختلف (عادی، مخفی‌کاری و هشدار) در شهر گشت بزنید.
برای مشاهده‌ی گیم پلی بازی میتوانید فایل ویدیو داخل ریپوزیتوری (batman_gameplay.mp4) را مشاهده کنید.

### ویژگی‌های اصلی:
- کنترل کامل حرکت بتموبیل با دو سطح سرعت
- سه حالت عملیاتی: عادی، مخفی‌کاری و هشدار
- سیستم Bat-Signal با امکان روشن/خاموش کردن و حرکت در آسمان
- نورهای چشمک‌زن قرمز و آبی در حالت هشدار
- افکت صوتی آژیر در حالت Alert
- سیستم نورپردازی دینامیک بسته به حالت‌های مختلف

---

## دستورالعمل‌های اجرای بازی

### پیش‌نیازها:
- Unity 2021.3 یا بالاتر
- Git برای کلون کردن پروژه

### مراحل اجرا:
برای اجرای بازی میتوانید پروژه را کلون کرده و آن را در یونیتی اجراکنید، یا فایل بیلد پروژه (Batman_build.zip) را اجرا کنید.

---

## راهنمای کنترل‌ها ⌨️

### حرکت بتموبیل:
| کلید | عملکرد |
|------|--------|
| `W` یا `↑` | حرکت به جلو |
| `S` یا `↓` | حرکت به عقب |
| `A` یا `←` | چرخش به چپ |
| `D` یا `→` | چرخش به راست |
| `Left Shift` | بوست سرعت (افزایش سرعت موقت) |

### تغییر حالت‌ها:
| کلید | حالت | توضیحات |
|------|------|---------|
| `N` | **Normal** (عادی) | حالت پیش‌فرض با سرعت و نور عادی، چراغ‌ها سفید روشن |
| `C` | **Stealth** (مخفی‌کاری) | سرعت کاهش یافته، چراغ‌ها کم‌نور می‌شوند |
| `Space` | **Alert** (هشدار) | نورهای چشمک‌زن قرمز و آبی + صدای آژیر فعال می‌شود |

### کنترل Bat-Signal:
| کلید | عملکرد |
|------|--------|
| `B` | روشن/خاموش کردن Bat-Signal |

**نکته:** وقتی Bat-Signal روشن است، به‌طور خودکار در آسمان حرکت می‌کند.

---

## خلاصه کار انجام‌شده

### 1. سیستم حرکت بتموبیل
- پیاده‌سازی حرکت فیزیک‌محور با `Rigidbody2D`
- حرکت جلو/عقب با کلیدهای `W/S` یا پیکان‌ها
- چرخش چپ/راست با کلیدهای `A/D` یا پیکان‌ها
- دو سطح سرعت: عادی و بوست (با نگه‌داشتن Shift)
- کنترل روان و واکنش‌گرا

### 2. سیستم مدیریت حالت‌ها (State Management)
- تعریف `enum BatmanState` با سه حالت: Normal, Stealth, Alert
- متغیر `currentState` برای ردیابی حالت فعلی
- تغییر حالت با کلیدهای مشخص (N, C, Space)
- تأثیر هر حالت روی سرعت و نورپردازی:
  - **Normal**: سرعت عادی، چراغ سفید روشن
  - **Stealth**: سرعت کم، چراغ کم‌نور
  - **Alert**: سرعت عادی، چراغ‌های چشمک‌زن قرمز/آبی + آژیر

### 3. سیستم نورپردازی
- استفاده از `Light2D` برای چراغ‌های بتموبیل
- تغییر رنگ و شدت نور بسته به حالت
- افکت چشمک‌زن در حالت Alert (هر 0.2 ثانیه بین قرمز و آبی)
- پیاده‌سازی Bat-Signal با نور قابل کنترل

### 4. سیستم Bat-Signal
- روشن/خاموش کردن با کلید `B`
- حرکت خودکار در آسمان (چرخش سینوسی)
- استفاده از `Light2D` برای نور سیگنال
- مدیریت نمایش گرافیکی همزمان با نور

### 5. سیستم صوتی
- پخش صدای آژیر در حالت Alert
- توقف صدا هنگام خروج از حالت Alert
- استفاده از `AudioSource` برای مدیریت صدا

---

## ساختار فایل‌های پروژه 📁

```
Batman/
├── Assets/
│   ├── Scenes/
│   │   └── SampleScene.unity          # صحنه اصلی بازی
│   ├── Scripts/
│   │   ├── BatmobileController.cs     # کنترل حرکت و حالت‌های بتموبیل
│   │   └── BatmanSignalController.cs  # کنترل Bat-Signal
│   ├── Photos/
│   │   └── batman.png                 # تصاویر پروژه
│   └── Settings/
│       ├── UniversalRP.asset          # تنظیمات رندرینگ
│       └── ...
├── ProjectSettings/                    # تنظیمات یونیتی
└── README.md                          # این فایل
```

---

## جزئیات فنی 🔧

### اسکریپت BatmobileController:
- **حرکت**: استفاده از `Rigidbody2D.linearVelocity` برای حرکت روان
- **چرخش**: استفاده از `Rigidbody2D.MoveRotation` برای چرخش فیزیک‌محور
- **State Machine**: پیاده‌سازی ساده با `switch-case` و `if/else`
- **نور چشمک‌زن**: استفاده از تایمر برای تغییر رنگ نور هر 0.2 ثانیه

### اسکریپت BatmanSignalController:
- **حرکت**: استفاده از `Mathf.Sin` برای چرخش سینوسی طبیعی
- **کنترل**: Toggle ساده با `enabled` برای روشن/خاموش کردن
- **سینکرونایز**: هماهنگی نور و گرافیک `SpriteRenderer`

---

## نکات مهم ⚠️

1. این پروژه از **Universal Render Pipeline (URP)** استفاده می‌کند
2. از **Light2D** برای نورپردازی 2D استفاده شده است